node_modules/
dist/
*.log
.env
.env.*
.DS_Store
# Local maintainer notes and scratch files (public repo, never commit).
# *.local.* covers notes (.md) and throwaway scripts (.mjs, .sh) alike.
.local/
*.local.*
audit.md
# Local Intent Guard state (contracts and runtime files belong in consuming app repos)
.conductor/
.cursor/
